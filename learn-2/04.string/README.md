String is a simple type, but it is immutable. You can't change it after created.
If you replace a string, it will create a new string. and garbage collector will collect the old string.

I don't want to write too complex example that I will forget anyway. LOL